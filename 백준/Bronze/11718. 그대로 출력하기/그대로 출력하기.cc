#include <bits/stdc++.h>
using namespace std;

int n, ret;
string s, a, b;
char c;
map<char, int> mp;
int main()
{
    // ios_base::sync_with_stdio(false);
    // cin.tie(NULL); cout.tie(NULL);
    while(1){
        getline(cin, s);
        if(s == "")break;
        cout << s << '\n';
    }
    
    return 0;
}
