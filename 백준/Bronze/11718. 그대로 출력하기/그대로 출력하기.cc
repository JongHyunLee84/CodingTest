#include <bits/stdc++.h>
using namespace std;

string a;

int main()
{
    cin.tie(NULL); cout.tie(NULL);
    while(getline(cin, a)){
        cout << a << '\n';
    }
    
    return 0;
}
